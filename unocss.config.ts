import { defineConfig, toEscapedSelector as e } from "unocss"

export default defineConfig({
    theme: {
        colors: {
            muse: "#e4007f",
            aqours: "#009fe8",
            niji: "#fab920",
            treelar: "#3399ff"
        }
    }
})